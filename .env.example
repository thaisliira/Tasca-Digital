# === Banco de Dados (PostgreSQL) ===
DB_USER=user
DB_PASSWORD=password
DB_NAME=notification_db
DB_HOST=db
DB_PORT=5432

# === Autenticação (JWT) ===
# Troque por uma chave longa e aleatória em produção (ex: openssl rand -hex 32)
JWT_SECRET=troca_isto_por_uma_chave_aleatoria_grande
APP_ENV=developmentConfirm: strip everything (including the db.go migration note and JSX section markers), or carve out exceptions?

# === Frontend (apenas Docker compose) ===
API_URL=http://backend:8080
NEXT_PUBLIC_API_URL=http://localhost:8080
